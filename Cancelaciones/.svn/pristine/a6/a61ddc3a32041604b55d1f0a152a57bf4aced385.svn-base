using System;
using System.Net;
using System.ServiceModel;

namespace ATL.SVC.Services
{
    // NOTE: You can use the "Rename" command on the "Refactor" menu to change the interface name "ICancelaciones" in both code and config file together.
    [ServiceContract]
    public interface ICancelaciones
    {
        //[OperationContract]
        //void DoWork();

        [OperationContract]
        HttpStatusCode Atl_2_Kco(string poliza, Guid clientId);
       

    }
}
